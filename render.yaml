services:
  - type: web
    name: PiggyBot
    env: python
    repo: https://github.com/Rishi2674/PiggyBot
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -b 0.0.0.0:$PORT --factory app:create_app
    envVars:
      - key: PORT
        value: 10000
